// ExampleExistingFile.js
import React from 'react';
import ReactDOM from 'react-dom';
import ExampleComponent from './ExampleComponent'; // Adjust the path as per your project structure

// Render the React component
ReactDOM.render(<ExampleComponent />, document.getElementById('root')); // Replace 'root' with the ID of the element where you want to render the component

